version: '3'

services:
  postgres:
    container_name: postgres
    image: postgres:14-alpine
    restart: always
    env_file:
      - game_app/.env
    expose:
      - 5432
    networks:
      - custom


  pgadmin:
    image: dpage/pgadmin4
    container_name: pgadmin4
    restart: always
    env_file:
      - game_app/.env
    ports:
      - "5050:5050"
    networks:
      - custom


  game_app:
    container_name: game_app
    image: game_app
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - game_app/.env
    depends_on:
      - postgres
    expose:
      - 8000
    networks:
      - custom


  nginx:
    container_name: nginx
    image: nginx:alpine
    ports:
      - "8082:80"
    volumes:
      - ./game_app/media:/game_app/media
      - ./nginx:/etc/nginx/conf.d
    depends_on:
      - game_app
      - postgres
    networks:
      - custom


networks:
  custom:
    driver: bridge
